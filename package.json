{
  "name": "agentic-organization",
  "private": true,
  "type": "module",
  "//": "Root deploy layer only. The website itself lives in site/ (its own package.json); `npm run dev` there for local work. This root wraps `build` + `deploy` so Cloudflare Workers (and one-command local deploys) can build the site and upload site/dist as static assets. See docs/deploy-cloudflare.md.",
  "scripts": {
    "build": "npm --prefix site install && npm --prefix site run build",
    "deploy": "npm run build && wrangler deploy",
    "dashboard": "node scripts/dashboard-data.mjs",
    "deploy:dashboard": "npm run dashboard && wrangler deploy --config apps/dashboard/wrangler.jsonc",
    "check": "node scripts/check-freshness.mjs && node scripts/check-workspace.mjs",
    "test": "node --test scripts/test-todo.mjs",
    "todos:dev": "node scripts/todos-dev.mjs",
    "todos:build": "npm --prefix apps/todos install && npm --prefix apps/todos run build",
    "deploy:todos": "npm run todos:build && wrangler deploy --config apps/todos/wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
